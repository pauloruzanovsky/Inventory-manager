<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title>Delete Supplier</title>
</head>
<body>
    <%- include('sidebar'); -%>
    <main>
    <h1><%=supplier.name%>s </h1>
    <p><%=supplier.description%></p>
    <% if(supplier_items.length > 0) { %>
    <p>Delete the following items before attempting to delete this supplier.</p>
    <% for (var i = 0; i < supplier_items.length; i++) { %>
    <p><a href="/inventory/item/<%=supplier_items[i].id%>"><%=supplier_items[i].name%></a> from <a href="/inventory/supplier/<%=supplier_items[i].supplier._id%>"><%=supplier_items[i].supplier.name%></a></p>
    <% } %>
    <% } else { %>
        <p>Do you really want to delete this supplier?</p>
        <form method="POST">
            <div class="form-group">
                <input type="hidden" id="supplierid" class="form-control" name="supplierid" required="true" value=<%=supplier._id%>>
            </div>
            <button type="submit" class="btn btn-primary">Delete</button>
        </form>
    <% } %>
    </main> 
</body>
</html>